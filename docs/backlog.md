# Backlog

Rows are possibilities, not promises. Use `Target Release` only after an item is committed to a release; a blank target means no commitment. `ID` is the next unused `C-NNN`; a retired ID stays retired, since commits, specs and receipts cite it.

`Kind` is one of `feature` (behavior still to be settled, of any size; at slice size it is a story and gets a spec), `bug` (actual behavior deviates from behavior already stated — `Item` states expected and actual, `Evidence` how to reproduce; fixed directly, no spec), or `chore` (maintenance changing no agreed behavior).

`Status` is one of `Candidate` (committed to no release) · `Scoped` (selected into a release) · `Specified` (a feature with a spec) · `Shipped` · `Rejected`. A blocked row is still a candidate: leave its status alone and name the missing prerequisite in `Evidence`.

| ID | Kind | Item | Evidence | Source | Status | Target Release |
|---|---|---|---|---|---|---|
| C-001 | feature | Floating always-on-top pet window (macOS, transparent, small) | Core value in charter | charter harvest | Specified | 001 |
| C-002 | feature | Claude Code CLI integration (reads activity/status via what the agent exposes) | Day-one agent constraint | charter harvest | Specified | 001 |
| C-003 | feature | Codex CLI integration | Day-one agent constraint | charter harvest | Specified | 001 |
| C-004 | feature | Short "what the agent is working on" status line beside the pet | Goal: state = short activity status | charter harvest | Specified | 001 |
| C-005 | feature | Attention states — waiting-for-input and errored are tellable from working at a glance, without reading text | Goal: glanceable state | charter harvest (pet split out to C-020, spec round story 004) | Specified | 001 |
| C-006 | feature | One status row per live session, each session distinguishable | Charter goal: concurrent sessions each with their own clear signal | charter harvest (corrected at scope 001; pet moved to C-005 at implement round) | Specified | 001 |
| C-007 | feature | Sound or attention cue when an agent flips to waiting-for-input or error | Developer idea | charter harvest | Candidate | — |
| C-008 | feature | Click pet to focus that session's terminal window/tab | Developer idea | charter harvest | Candidate | — |
| C-009 | feature | Menu-bar / status-bar variant of the status | Developer idea | charter harvest | Candidate | — |
| C-010 | feature | Adjustable pet size | Goal: stays out of the way | charter harvest | Candidate | — |
| C-011 | feature | Toggle: pet visible with status line hidden | Goal: stays out of the way | charter harvest | Candidate | — |
| C-012 | feature | Live-session discovery — show only agent sessions actually running, drop stale ones | Both agents leave transcripts on disk after a session ends; naive discovery would show stale pets | scope 001 | Specified | 001 |
| C-013 | feature | Pet expression summarizes the most urgent state across all live sessions (error > waiting > working) | Saves scanning rows when many sessions are live; cut from 001 as redundant with per-row state, uncut at spec round 004 once the pet itself turned out to be unbuilt — absorbed into C-020 | scope 001 (reversed, spec round story 004); specified as story 005 | Specified | 001 |
| C-014 | feature | Add-directory picker — user adds agent directories to watch via a folder chooser | Sessions live under multiple profile directories (observed: ~/.claude and a second, non-default profile directory); defaults alone can't cover a third | spec round, story 001 | Scoped | 001 |
| C-015 | feature | Persist profile directories the pet learned from live processes, instead of relearning each tick | Learned directories are in-memory only in 001; persisting them survives a restart with no session running | design round, story 001 | Candidate | — |
| C-016 | feature | Pet window is placeable and controllable — drag to move, position remembered, menu bar icon for show/hide and quit | Pet is unusable fixed at top-right; with clicks no longer passing through it must be movable, and an accessory app with no dock icon has no visible way to be closed | build, story 001 | Specified | 001 |
| C-017 | feature | Status age counts from when the status actually changed, not from when the pet first observed it — Claude from `statusUpdatedAt` and the error entry's `timestamp`, Codex from its turn boundary's `timestamp` | Claude Code writes `statusUpdatedAt` on every registry file and the adapter never reads it; observed live — a session idle 96 minutes displayed as 0s after relaunch. Codex's rollout boundary lines carry their own `timestamp`, found at the spec round of story 006, so the same rule reaches both agents | spec round, story 002; specified as story 006 | Specified | 001 |
| C-018 | feature | Per-agent icon or colour on each session row, instead of the agent's name as text | Glanceability is the release goal and an icon reads faster than a word in a narrow row; deferred because it needs the pet to hold a table of agent names, which the agent-agnostic premise forbids — the honest form is an adapter supplying its own display token | spec round, story 003 | Candidate | — |
| C-019 | feature | Sprite-sheet or raster-animated pet, instead of drawn vector paths | A richer creature than vector paths can express; deferred because it needs an asset pipeline the hand-rolled `build.sh` has no place for, and vector scales to any display with no assets | spec round, story 004 | Candidate | — |
| C-020 | feature | The pet itself — a drawn creature below the rows, offset left or right by where it sits on the display, with the status rows as a speech bubble that switches sides at the screen border | The charter's central noun is unbuilt: the surface is a list of text with no pet on it. Split from C-005 at refinement so the attention states can ship without waiting on the visual work | spec round, story 004; specified as story 005 | Specified | 001 |
| C-021 | feature | Clicking inside the bubble does something — a row, the agent name, the activity line each acting on its session | Story 005 makes the bubble catch clicks and do nothing with them, so the surface has click targets with no behaviour; what each should do is unsettled and C-008 (focus the terminal) is one candidate answer | spec round, story 005 | Candidate | — |
| C-022 | feature | A finished vector creature — a properly drawn pet replacing story 005's placeholder | Story 005 ships a simple placeholder that carries the five expressions and sleep; a proper vector creature was judged too much work to squeeze into that story. Observed at story 005's manual checks: the working face — half-lidded eyes over a flat mouth — reads as miserable rather than as busy, and it was misread on sight by the developer who picked the creature. Working is the state the pet is in most of the time, so the placeholder wears a mildly unhappy face all day. Distinct from the other four, which is all story 005 asked for, but not yet expressive of the right thing. Also observed there: the sleeping creature's Zs are unreadable over a dark background, because they are the only marks drawn outside the body — every body part is a paper fill under an ink outline and so holds over anything, while the Zs are bare ink strokes over whatever is behind them. The creature prototype carried a halo option for exactly this (a paper-coloured stroke laid under each ink one, its `h` key) and story 005's build dropped it when trimming the prototype down; any finished creature with marks outside its body needs it back | spec round, story 005; sharpened at build | Candidate | — |
| C-023 | chore | Two windows instead of one — a bubble panel and a creature panel kept in lockstep, so every point of each window is drawn and the transparent region belongs to no window at all | Story 005's surface is one window with a wide transparent band, and what a click means there is a cached `ignoresMouseEvents` that every mover has to invalidate. Three review rounds each found a different mover it had been missed from: the gap itself, the bubble growing under a motionless pointer, the drawn tail, and AppKit relocating the window on a display change. Each fix was correct; the pattern is that there is always one more mover, and two windows have no cache to invalidate | build, story 005 | Candidate | — |
| C-026 | feature | The bubble's side flip reads smoothly instead of as a snap | Story 005's flip is live — photographed mid-drag with the button still down, already swung across before release — but instant, jumping the bubble its full width in one frame, which the developer read as a snap. The discontinuity is inherent, not a shortcut: the bubble has no valid position between the creature's two sides, and the flip fires when it is already flush against the screen edge. The obvious fix does not work either — the window is exactly the bubble's width, so animating the bubble inside it slides it into its own clip. Sliding it visibly means widening the window mid-drag to span both positions and narrowing after, which also moves the click-through regions during a gesture; a cross-fade is cheap but likely trades a snap for a blink | build, story 005 | Candidate | — |
| C-027 | feature | Time-since-last-activity surfaced as its own signal, separate from the status age | Story 006 makes the age mean "how long this state has lasted", which is the right meaning but drops the one thing the old behaviour carried: a working row stuck at `Working 2m 14s` meant the agent had made no tool call for two minutes. After 006 a hung session is indistinguishable from a busy one until the age gets implausible. Form unsettled — a second smaller number, a dot that changes when a working session goes quiet, or nothing if real use says it never mattered; note that any threshold-based form runs into the tuned-staleness objection stories 001 and 004 both rejected, so the non-threshold forms are the interesting ones. Cost named and accepted by the developer at Q2 of story 006's alignment round | spec round, story 006 | Candidate | — |
